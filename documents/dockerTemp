docker run -p 11111:3306 -v concertly-legacy-db:/var/lib/mysql --name concertly-legacy-db -e MYSQL_ROOT_PASSWORD=${rootPassword} -d mariadb
docker run -p 11112:3306 -v concertly-test-db:/var/lib/mysql --name concertly-test-db -e MYSQL_ROOT_PASSWORD=${rootPassword} -d mariadb

